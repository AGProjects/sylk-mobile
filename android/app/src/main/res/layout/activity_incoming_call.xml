<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/incomingCallRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:padding="16dp">

    <!-- Push-notification-style bubble at the top of the screen -->
    <LinearLayout
        android:id="@+id/notificationBubble"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="48dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:orientation="horizontal"
        android:background="@drawable/notification_bubble_bg"
        android:padding="14dp"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/sylk_logo"
            android:layout_marginEnd="12dp"
            android:contentDescription="Sylk"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/callerNameText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="John Doe"
                android:textColor="@android:color/white"
                android:textSize="15sp"
                android:textStyle="bold"
                android:singleLine="true"
                android:ellipsize="end"/>

            <TextView
                android:id="@+id/callingLabelText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="is calling"
                android:textColor="#D1D1D6"
                android:textSize="13sp"/>

            <TextView
                android:id="@+id/callerUriText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="#8E8E93"
                android:textSize="12sp"
                android:singleLine="true"
                android:ellipsize="middle"
                android:layout_marginTop="2dp"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Sylk logo centered on the screen -->
    <ImageView
        android:id="@+id/appLogo"
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:src="@drawable/sylk_logo"
        android:layout_centerInParent="true"
        android:contentDescription="Sylk Logo"/>

    <!-- Single Accept / Decline pair (audio is the default; the action sent is
         ACTION_ACCEPT_VIDEO when the incoming call's media-type is video, so
         the in-app side picks the right path) -->
    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="80dp">

        <Button
            android:id="@+id/declineButton"
            android:layout_width="140dp"
            android:layout_height="48dp"
            android:text="Decline"
            android:textColor="@android:color/white"
            android:background="@drawable/btn_red_rounded"
            android:layout_marginEnd="16dp"/>

        <Button
            android:id="@+id/acceptButton"
            android:layout_width="140dp"
            android:layout_height="48dp"
            android:text="Accept"
            android:textColor="@android:color/white"
            android:background="@drawable/btn_green_rounded"
            android:layout_marginStart="16dp"/>
    </LinearLayout>

</RelativeLayout>
