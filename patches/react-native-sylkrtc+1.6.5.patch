diff --git a/node_modules/react-native-sylkrtc/lib/call.js b/node_modules/react-native-sylkrtc/lib/call.js
index af962e0..c32d44e 100644
--- a/node_modules/react-native-sylkrtc/lib/call.js
+++ b/node_modules/react-native-sylkrtc/lib/call.js
@@ -155,8 +155,10 @@ class Call extends EventEmitter {
         // Create the RTCPeerConnection
         this._initRTCPeerConnection(pcConfig);
 
-        this._pc.addStream(options.localStream);
-        this.emit('localStreamAdded', options.localStream);
+		options.localStream.getTracks().forEach((track) => {
+			this._pc.addTrack(track, options.localStream);    
+		});
+
         this._pc.setRemoteDescription(new RTCSessionDescription({type: 'offer', sdp: this._incomingSdp}))
             // success
             .then(() => {
@@ -266,65 +268,73 @@ class Call extends EventEmitter {
         this._sendTerminate();
     }
 
-    sendDtmf(tones, duration=100, interToneGap=70) {
-        DEBUG('sendDtmf()');
-        // if (this._dtmfSender === null) {
-        //     if (this._pc !== null) {
-        //         let track = null;
-        //         try {
-        //             track = this._pc.getLocalStreams()[0].getAudioTracks()[0];
-        //         } catch (e) {
-        //             // ignore
-        //         }
-        //         if (track !== null) {
-        //             DEBUG('Creating DTMF sender');
-        //             this._dtmfSender = this._pc.createDTMFSender(track);
-        //             if (this._dtmfSender) {
-        //                 this._dtmfSender.addEventListener('tonechange', this._onDtmf);
-        //             }
-        //         }
-        //     }
-        // }
-        if (true) {
-            DEBUG('Sending DTMF tones');
-            this._pc.sendDTMF(tones, duration, interToneGap);
-        }
-    }
-
-    // Private API
-
-    _initOutgoing(uri, options={}) {
-        if (uri.indexOf('@') === -1) {
-            throw new Error('Invalid URI');
-        }
-
-        if (!options.localStream) {
-            throw new Error('Missing localStream');
-        }
-
-        this._id = options.id || uuidv4();
-        this._direction = 'outgoing';
-        this._remoteIdentity = new utils.Identity(uri);
-        this._headers = options.headers;
-
-        const pcConfig = options.pcConfig || {iceServers:[]};
-        const offerOptions = options.offerOptions;
+	sendDtmf(tones, duration = 100, interToneGap = 70) {
+		DEBUG('sendDtmf()');
+
+		if (!this._dtmfSender) {
+			DEBUG('DTMF sender NOT available');
+			return false;
+		}
+
+		DEBUG(`Sending DTMF: ${tones}`);
+		this._dtmfSender.insertDTMF(tones, duration, interToneGap);
+
+		return true;
+	}
+
+	// Private API
+	_initOutgoing(uri, options = {}) {
+		if (!uri.includes('@')) throw new Error('Invalid URI');
+		if (!options.localStream) throw new Error('Missing localStream');
+	
+		this._id = options.id || uuidv4();
+		this._direction = 'outgoing';
+		this._remoteIdentity = new utils.Identity(uri);
+		this._headers = options.headers;
+		this._audioSender = null;
+	
+		const pcConfig = options.pcConfig || {
+			iceServers: [{ urls: 'stun:stun.l.google.com:19302' }]
+		};
+		const offerOptions = options.offerOptions;
+	
+		// Create RTCPeerConnection
+		this._initRTCPeerConnection(pcConfig);
+	
+		// ❗ M111: use addTrack instead of addStream
+		options.localStream.getTracks().forEach((track) => {
+			const sender = this._pc.addTrack(track, options.localStream);
+			if (track.kind === 'audio') {
+				this._audioSender = sender;
+			}
+		});
+	
+		this.emit('localStreamAdded', options.localStream);
+	
+		// Create the offer
+		utils.createLocalSdp(this._pc, 'offer', offerOptions, 'VP8')
+			.then((sdp) => {
+				DEBUG('Local SDP: %s', sdp);
+				//
+				// ✅ DTMF becomes available here — AFTER setLocalDescription()
+				//
+				setTimeout(() => {
+					if (this._audioSender?.dtmf) {
+						DEBUG("DTMF sender now available");
+						this._dtmfSender = this._audioSender.dtmf;
+						this._dtmfSender.addEventListener('tonechange', this._onDtmf);
+					} else {
+						DEBUG("No DTMF sender even after negotiate");
+					}
+				}, 0);
+				this._sendCall(uri, sdp);
+			})
+			.catch((reason) => {
+				DEBUG(reason);
+				this._localTerminate(reason);
+			});
+	}
 
-        // Create the RTCPeerConnection
-        this._initRTCPeerConnection(pcConfig);
-
-        this._pc.addStream(options.localStream);
-        this.emit('localStreamAdded', options.localStream);
-        utils.createLocalSdp(this._pc, 'offer', offerOptions, 'VP8')
-            .then((sdp) => {
-                DEBUG('Local SDP: %s', sdp);
-                this._sendCall(uri, sdp);
-            })
-            .catch((reason) => {
-                DEBUG(reason);
-                this._localTerminate(reason);
-            });
-    }
 
     _initIncoming(id, caller, sdp, callId, headers = {}) {
         this._id = id;
@@ -333,6 +343,8 @@ class Call extends EventEmitter {
         this._direction = 'incoming';
         this._state = 'incoming';
         this._callId = callId;
+		this._dtmfSender = null;
+
         this._remoteMediaDirections = Object.assign(
             {audio: [], video:[]}, utils.getMediaDirections(sdp)
         );
@@ -448,26 +460,34 @@ class Call extends EventEmitter {
         }
     }
 
-    _initRTCPeerConnection(pcConfig) {
-        if (this._pc !== null) {
-            throw new Error('RTCPeerConnection already initialized');
-        }
+	_initRTCPeerConnection(pcConfig) {
+		if (this._pc !== null) {
+			throw new Error('RTCPeerConnection already initialized');
+		}
+	
+		this._pc = new RTCPeerConnection(pcConfig);
+
+		this._pc.addEventListener('track', (event) => {
+			const stream = event.streams && event.streams[0];
+			if (stream) {
+				this.emit('streamAdded', stream);
+			} else {
+				DEBUG('Track added without stream');
+			}
+		});
+
+		this._pc.addEventListener('icecandidate', (event) => {
+			if (event.candidate) {
+				DEBUG('New ICE candidate', event.candidate);
+			} else {
+				DEBUG('ICE candidate gathering finished');
+			}
+	
+			this._sendTrickle(event.candidate);
+		});
 
-        this._pc = new RTCPeerConnection(pcConfig);
-        this._pc.addEventListener('addstream', (event) => {
-            DEBUG('Stream added');
-            this.emit('streamAdded', event.stream);
-        });
-        this._pc.addEventListener('icecandidate', (event) => {
-            if (event.candidate !== null) {
-                DEBUG('New ICE candidate %o', event.candidate);
-            } else {
-                DEBUG('ICE candidate gathering finished');
-            }
-            this._sendTrickle(event.candidate);
-        });
         this._statistics.addConnection({pc:this._pc, peerId: this._id});
-    }
+	}
 
     _sendRequest(req, cb) {
         this._account._sendRequest(req, cb);
diff --git a/node_modules/react-native-sylkrtc/lib/conference.js b/node_modules/react-native-sylkrtc/lib/conference.js
index 33da09d..58e623b 100644
--- a/node_modules/react-native-sylkrtc/lib/conference.js
+++ b/node_modules/react-native-sylkrtc/lib/conference.js
@@ -121,10 +121,15 @@ class Participant extends EventEmitter {
         const pc = new RTCPeerConnection(pcConfig);
         this._conference._statistics.addConnection({pc: pc, peerId: this._id});
 
-        pc.addEventListener('addstream', (event) => {
-            DEBUG('Stream added');
-            this.emit('streamAdded', event.stream);
-        });
+		this._pc.addEventListener('track', (event) => {
+			const stream = event.streams && event.streams[0];
+			if (stream) {
+				this.emit('streamAdded', stream);
+			} else {
+				DEBUG('Track added without stream');
+			}
+		});
+    
         pc.addEventListener('icecandidate', (event) => {
             if (event.candidate !== null) {
                 DEBUG('New ICE candidate %o', event.candidate);
@@ -572,7 +577,10 @@ class ConferenceCall extends EventEmitter {
             this._sendTrickle(event.candidate);
         });
 
-        this._pc.addStream(options.localStream);
+		options.localStream.getTracks().forEach((track) => {
+			this._pc.addTrack(track, options.localStream);
+		});
+
         this.emit('localStreamAdded', options.localStream);
         DEBUG('Offer options: %o', offerOptions);
         utils.createLocalSdp(this._pc, 'offer', offerOptions)
