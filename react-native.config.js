module.exports = {
    // Fonts for the icon families are self-linked by the per-family
    // @react-native-vector-icons packages (iOS pod resources + Android
    // gradle copyFonts), so no manual asset linking is needed here.
    assets: [],
    dependencies: {
    },
};